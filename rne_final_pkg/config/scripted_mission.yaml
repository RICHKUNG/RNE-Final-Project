# Config for scripted_final_mission (no Nav2 — localization-assisted scripted route).
# Coordinates are map-frame placeholders: calibrate against the stored map before a real run.

init:
  # Max seconds to wait in INIT for localization pose + required YOLO topics.
  # On timeout the mission FAILS loudly instead of starting blind.
  wait_timeout_s: 600
  # Pose older than this (seconds) counts as lost → mission stops and complains.
  pose_stale_s: 2.0
  # Hands-off start: if AMCL has no pose after initial_pose_wait_s, publish
  # /initialpose with the values below (map frame), repeating until localized.
  # Spawn assumed at map origin (build_map starts SLAM there); yaw matches
  # route.start_heading_yaw. Verify against the stored map before a real run.
  initial_pose_wait_s: 3.0
  initial_pose_x: 0.0
  initial_pose_y: 0.0
  initial_pose_yaw: 0.0486   # rad

route:
  # Route to the door (measured 2026-06-12, map frame):
  #   spawn → straight → turn_point → left 90° → straight → door_lane_point
  #         → right 90° → short straight → door_front → knob visual servo
  start_heading_yaw: 0.0486   # rad — heading at spawn (reference for calibration)

  turn_point:          # measured: end of the first straight leg from spawn
    x: 3.0378860404780157
    y: 0.025078211891774258
    yaw: 0.0486        # rad — heading on arrival; left-turn target = yaw + pi/2
    tolerance: 0.20
  door_lane_point:     # PROVISIONAL calibration value, not measured:
    x: 3.0378860404780157   #   x = turn_point.x
    y: 1.6131596549874418   #   y = door_front.y
    tolerance: 0.20         # replace with the measured second-turn pose when available
  door_front:          # measured: standing point facing the door knob
    x: 3.058493898788231
    y: 1.6131596549874418
    yaw: 0.0341        # rad — final heading facing the knob (right-turn target)
    tolerance: 0.15
  door_exit_point:     # measured 2026-06-12: hold point just past the opened door
    x: 3.5640017942719395
    y: 1.5840655836032285
    yaw: 0.0326        # rad — from measured quaternion (z=0.016320, w=0.999867)
    tolerance: 0.05    # tight: the push ends near this point, so a loose
                       # tolerance skips the positioning drive entirely
  # Ramp observe chains (measured 2026-06-12 from /amcl_pose, map frame). Each is
  # an ordered list walked top→bottom: drive to a point, RAMP_SCAN; if the ramp
  # isn't seen there, advance to the next point. Exhausting the long chain moves
  # to the short chain; exhausting both fails. yaw is radians (from the measured
  # quaternion; yaw_deg kept in the comment for reference).
  long_side_observe:   # car faces ≈ -x, scanning the ramp from the long side
    - x: 3
      y: 2.4
      yaw: -3.14    # yaw_deg -173.50
      tolerance: 0.25
    - x: 3
      y: 1.5
      yaw: -3.14     # yaw_deg -178.91
      tolerance: 0.25
    - x: 3
      y: 0.9
      yaw: -3.14     # yaw_deg -178.75
      tolerance: 0.25
  short_side_observe:  # car faces ≈ +y, scanning the ramp from the short side
    - x: 2.1
      y: 0
      yaw: 1.57      # yaw_deg 89.21
      tolerance: 0.25
    - x: 1.6
      y: 0
      yaw: 1.57     # yaw_deg 96.66
      tolerance: 0.25
    - x: 0.85
      y: 0
      yaw: 1.57      # yaw_deg 98.17
      tolerance: 0.25
  long_to_short_corner:  # Perimeter corner between the long and short observe chains.
    # Going straight from the long chain's last point to the short chain's first
    # point cuts a diagonal across the inner corner — straight over the bridge.
    # Instead route through this OUTER corner: drive down the right edge (x of the
    # long side) to the bottom edge (y of the short side), then the short-side
    # move turns to face +y. Two right-angle legs, no diagonal.
    x: 3.0    # ≈ long_side right edge x (matches long_side_observe last point)
    y: -0.046 # ≈ short_side bottom edge y (matches short_side_observe first point)
    tolerance: 0.25
  origin:              # TODO calibrate: drop-off / return point
    x: 0.0
    y: 0.0
    tolerance: 0.25

control:
  fast_speed: 700.0
  medium_speed: 450.0      # used for route legs
  slow_speed: 350.0        # used for visual servo / final approach
  turn_speed: 400.0
  turn_slow_speed: 400.0   # near-target rotation; 150 stalls in-place turns in Unity, 250 works
  turn_slowdown_deg: 20.0  # yaw error below this → rotate at turn_slow_speed (anti-overshoot)
  yaw_tolerance_deg: 3.0   # turn-in-place completion threshold, in DEGREES (plain number — YAML can't eval math.radians())
  forward_angle_deg: 20.0  # heading error below this → drive forward, else rotate
  xy_tolerance: 0.20       # default arrival tolerance when a waypoint omits one

stuck:
  # Active while commanding forward on route legs (drive_to_point).
  move_threshold_m: 0.03   # less displacement than this counts as not moving
  timeout_s: 2.0           # no movement for this long → back up and retry
  recover_backward_s: 1.0

safety:
  # Map +x beyond this is the sea (drive there and the car falls in). No route
  # waypoint exceeds it — reaching it means drift/overshoot, so the sea guard
  # catches it. Door states (DOOR_PRESS_COMMIT, TURN_FORWARD, BACK_UP_AFTER_TASK3)
  # are exempt: the door and door_exit_point (~x=3.56) legitimately sit past it.
  sea_x_limit: 3.4
  # Escape pulls x below (limit − this) before handing control back, so the car
  # doesn't immediately re-trigger sitting on the boundary.
  sea_clear_margin: 0.15

debug:
  # Set to a state name (e.g. TASK3_KNOB_SERVO) to jump there right after INIT,
  # skipping earlier states — for calibrating one phase at a time.
  # Empty = normal mission flow. Invalid names abort at startup.
  start_state: ""

knob_servo:
  target_depth_m: 0.50
  # Commit window is [target - tol, target]: farther → forward, closer than
  # (target - tol) → back up. The pre-press forward leg is computed dynamically
  # (commit depth − press_standoff_m), so committing anywhere in the window still
  # presses correctly — a wide window is fine. The window MUST be wider than the
  # per-tick travel (~0.02 m at slow_speed, 10 Hz) or bang-bang control overshoots
  # it every correction and oscillates forever (the old 0.03 m window did this).
  # 0.12 → window [0.38, 0.50]; lower bound stays safely above the 0.30 m standoff.
  depth_tolerance_m: 0.12
  # On reaching the window: stop, wait this long, re-read depth, commit only if
  # still in window. Kills message-lag/coast error (occasional press overshoot);
  # the settled depth also sizes the pre-press forward leg (see press_standoff_m).
  commit_settle_s: 0.5
  center_threshold_px: 40.0
  # STOP-AND-LOOK knob servo. Reads dx/depth only while stopped, then commits a
  # bounded open-loop burst so YOLO/camera latency cannot drive an oscillation.
  align_settle_s: 0.4
  align_turn_speed: 330.0
  align_turn_s_per_px: 0.0015
  align_turn_min_s: 0.15
  align_turn_max_s: 0.5
  align_forward_s: 0.15
  align_search_burst_s: 0.3
  max_seconds: 600.0          # total servo budget; exceeded → FAILED (fail loud)
  invalid_depth_fail_s: 2.0  # knob centered but depth invalid for this long → FAILED

door_press:
  # The hit pose is calibrated for a 0.30 m car-to-knob gap. The pre-press
  # forward leg is computed per run as (servo commit depth − press_standoff_m),
  # so servo-window variance no longer shifts the press distance.
  press_standoff_m: 0.30
  forward_before_press_m: 0.20   # fallback only: debug-jumping straight into
                                 # DOOR_PRESS has no servo commit depth
  forward_during_press_m: 0.12
  # Straight-line displacement of the arc push. Door opening angle ≈
  # 2*asin(d / (2*door_width)). Measured 2026-06-12 in Unity: actual door_width ≈ 0.56 m
  # (not 0.78 m). Calibration: 0.97 m total → 120°; 0.76 m total → ~85°.
  # 2026-06-13 field tune: 0.76 m / 3.0 s and 0.90 m / 4.0 s were both too short,
  # so raise the sanity distance to 1.12 m total and let the timed arc run longer.
  # forward_during_press_m (0.12) + push_after_unlock_m (1.00) = 1.12 m total.
  push_after_unlock_m: 0.9
  push_speed: 120.0          # straight segments: pre-press forward, retry backup
  # Arc push toward front-right while the claw holds the knob down. Hinge is
  # on the RIGHT (facing the door), handle on the LEFT, so the handle sweeps
  # front-right as the door swings open — the arc follows it. If the car turns
  # too sharply and slides off the door, raise push_speed_right toward
  # push_speed_left. TODO TUNE the ratio in Unity.
  # 150/90 stalled against the door's resistance (moved only 0.18 m of 1.12 m in
  # 13 s); scaled up ~2x keeping the 1.67 arc ratio so the car pushes through.
  push_speed_left: 450.0
  push_speed_right: 270.0
  # Arc push duration. While the claw holds the door the base barely translates
  # (door resists + wheels skid), so pose distance never reaches push_after_unlock_m
  # and phase 5 used to always run to phase_timeout_s (13 s) — pushing the door far
  # past 90°. Phase 5 now ends at this TIME instead, so the door angle = push_speed ×
  # push_seconds. TUNE: too far past 90° → lower push_seconds (or push_speed_*);
  # not open enough → raise it. push_after_unlock_m below is now just a sanity
  # early-out, not the primary terminator.
  push_seconds: 3.0
  retry_count: 0             # extra unconditional re-presses; door opens first try
  exit_wait_s: 0.0           # hold at route.door_exit_point after the door opens
  # Calibrated 2026-06-12 from /robot_arm echo (3 joints; arm_writer leaves the
  # rest, same as STOW_POS). Raw radians: raise [0.0, 2.4609142, 1.5707963],
  # press [0.9948377, 2.4609142, 1.5707963] = exactly the degrees below.
  arm_raise_deg:  [0.0, 141.0, 90.0]    # gripper at highest point, above the knob
  arm_press_deg:  [57.0, 141.0, 90.0]   # hit pose — just touches the knob at 0.30 m
  # TODO TUNE: low pose the press continues to AFTER the hit pose, driving the
  # knob fully down. Placeholder — joint0 deepened past 57°, not yet calibrated.
  arm_press_low_deg: [80.0, 141.0, 90.0]
  # Pose held DURING the arc push. The door has a top/bottom rod lock — the
  # handle must stay pressed while the door moves or it re-locks at a small
  # angle. Hold the FULL low (pressed-to-bottom) pose through the push rather
  # than lifting back to the hit pose — keeping the claw low keeps the lock
  # fully disengaged so the door swings wider. Same value as arm_press_low_deg.
  arm_push_hold_deg: [80.0, 141.0, 90.0]
  arm_settle_s: 1.2          # wait after each arm command
  phase_timeout_s: 13.0      # per-phase watchdog: pose-distance phases give up
                             # after this (sized for the ~1.12 m arc push; raise
                             # alongside push_after_unlock_m or it truncates early)

post_task3:
  # After the door opens, turn back to face forward (TASK3_TURN_FORWARD), then
  # reverse straight back this far (pose-measured) before scanning for the ramp —
  # clears the doorway so the ramp comes into the camera's view for the
  # observe/scan chain.
  turn_forward_timeout_s: 5.0  # give up the face-forward turn after this (still
                               # jammed on the door) and reverse anyway
  backup_distance_m: 0.7
  backup_speed: 400.0        # slow_speed; in-place reverse, no heading control

ramp:
  # Reference only — the model is loaded by yolo_example_pkg/object_detect.py from its
  # installed share dir (SEG_MODEL_FILENAME). Listed here so calibration notes stay together.
  model_path: "/home/howard/richkung/RNE/ros2_yolo_integration/src/yolo_example_pkg/models/ramp_yolo11n.pt"
  # Ramp scan uses the larger of:
  #   - bottom-area ratio (legacy /yolo/bridge_info[2])
  #   - full-frame mask ratio (/yolo/bridge_info[3])
  # This catches short-side observations where Foxglove shows the ramp mask
  # higher in the frame but the legacy bottom-half score is still near zero.
  found_area_threshold: 0.01
  # Observation-chain confirmation only: the ramp mask must overlap the central
  # 1/3 x 1/3 image box by at least this ratio of that box. This prevents a ramp
  # mask at the image edge from prematurely ending the observe chain.
  center_overlap_threshold: 0.01
  info_stale_s: 1.5
  # Units = distinct seg inferences from the timer-driven segmentation node,
  # NOT 10 Hz control ticks.
  found_required_frames: 2
  found_window_frames: 6
  # Outbound pre-check (SEGMENT_1, spawn→turn_point): the ramp is seen from far
  # away here so it fills much less of the frame than the close-range observe
  # scan — use looser thresholds (smaller area, fewer hits) just to set the
  # "bridge is horizontal" flag. Tune against the去程 log if it under/over-fires.
  outbound_area_threshold: 0.01
  outbound_required_frames: 2
  scan_seconds: 3.0         # max time scanning per observation side
  center_threshold_px: 50.0
  # RAMP_ALIGN_BOTTOM (runs once after a scan confirms the ramp, before the
  # approach): visual-servo the ramp mask's near edge down to the frame bottom.
  # /yolo/bridge_info[4] reports that edge as y/(h-1): 0=top, 1=frame bottom.
  align_bottom_target_ratio: 0.95   # edge ≥ this counts as aligned to the bottom
  align_center_threshold_px: 70.0    # wider than approach: square-up yaw can shift dx
  align_speed: 220.0                 # stronger short pulse; 120 can stall on ramp
  align_timeout_s: 15.0              # ramp not reacquired this long → go to approach anyway
  # STOP-AND-LOOK ramp alignment. Rotation stays above the Unity in-place stall
  # floor; turn amount is controlled by burst duration, not by lowering speed.
  align_settle_s: 0.4
  align_turn_speed: 330.0
  align_turn_s_per_px: 0.0015
  align_turn_s_per_skew: 2.2
  align_turn_min_s: 0.15
  align_turn_max_s: 0.5
  align_forward_s: 0.15
  align_search_burst_s: 0.3
  align_info_stale_s: 1.5
  # Launch gate from /yolo/bridge_align. Require square and uncropped before
  # handing off to RAMP_APPROACH.
  align_min_shape_conf: 0.4
  align_skew_tolerance: 0.12
  approach_speed: 260.0
  approach_forward_s: 0.15
  approach_done_area: 0.15   # ramp fills this much of bottom half → close enough, classify bears
  approach_timeout_s: 300.0

bear:
  # Classification: blocking bear = low in frame AND close.
  blocking_pixel_y_threshold: 360.0
  blocking_depth_threshold_m: 1.0
  # Reserved: /yolo/bear_info has no area field yet (TODO if classification needs it).
  blocking_area_threshold: 0.05
  classify_observe_seconds: 1.5
  align_threshold_px: 50.0
  # Maximum/cap alignment band applied ONLY at grab depth. The runtime gate uses
  # grab_align_tol_m + camera fx to convert a physical lateral tolerance to pixels,
  # then floors/caps it with grab_align_min_px/grab_align_threshold_px.
  # 2026-06-13: tightened 75->50 — 75px let the grab fire too far off-centre;
  # require the bear within 50px of frame centre before grabbing.
  grab_align_threshold_px: 50.0
  grab_align_tol_m: 0.015
  grab_align_min_px: 15.0
  # STOP-AND-LOOK bear servo (deadtime-immune). The camera→YOLO chain lags ~0.3 s,
  # so any move-while-watching loop (bang-bang or PD) commands against a stale dx
  # and limit-cycles — untunable, the deadtime is too large. Instead the car only
  # reads dx/depth while STOPPED AND SETTLED, then commits ONE open-loop burst
  # (rotate toward the bear, or a short forward creep), stops, and re-measures.
  # Cycle: SETTLE → (TURN if |dx|>align_threshold_px) → SETTLE → (FORWARD if not
  # yet at grab depth) → … → grab once aligned AND at depth.
  #
  # Settle MUST exceed the camera latency, else the read is still stale.
  align_settle_s: 0.4
  # In-place rotate speed for a TURN burst (must clear the Unity ~250 stall floor).
  # 330 needed on the ramp where gravity raises the effective stall threshold.
  align_turn_speed: 330.0
  # TURN burst duration = clamp(|dx| * align_turn_s_per_px, min, max). Bigger
  # s_per_px = more turn per pixel (faster convergence, risk of overshooting the
  # band in one step → an extra back-and-forth settle). Tune so a centred-ish dx
  # lands inside align_threshold_px in 1-2 bursts.
  align_turn_s_per_px: 0.0015
  # Slightly above the 10 Hz control-loop period (0.1 s): _servo_enter_burst
  # publishes immediately, and this keeps the minimum turn small enough for the
  # tighter depth-scaled grab band to be reachable.
  align_turn_min_s: 0.12
  align_turn_max_s: 0.5
  # FORWARD creep burst durations are caps. Runtime also limits each burst by
  # remaining depth gap (d - grab_target_distance_m), using grab_creep_mps below.
  align_forward_min_s: 0.10
  align_forward_near_s: 0.15
  align_forward_far_s: 0.3
  # Reject one sudden "farther" depth read after we were already near; repeated
  # jumps are allowed to proceed only with near-branch creep sizing.
  depth_jump_reject_m: 0.15
  # Re-acquire rotate burst when the bear isn't visible after a settle.
  align_search_burst_s: 0.3
  # YOLO bear markers arrive in camera_optical_frame, where z is forward depth,
  # not vertical height. Keep mission-side offset at 0.0 so the clicked target
  # stays in front of the camera; arm_controller_2D applies a small post-transform
  # lowering in arm_ik_base for camera-frame targets.
  grab_z_offset_m: 0.0
  # Switch to careful near-branch creep once the bear is within this depth.
  # Keep this comfortably above grab_target_distance_m so noisy close-range
  # depth cannot leave the car in the large far/climb step.
  grab_distance_m: 0.55
  # Stop-and-look final approach: every settled frame sizes the next timed creep
  # from the remaining YOLO depth gap, then stops and re-measures. The car still
  # does not read vision while moving, but no single burst should be a 6 cm lunge
  # near the bear.
  # FIELD FINDING (2026-06-13): the body collides with the bear at ~0.33 m YOLO
  # depth (after which the bear is shoved out of frame). 0.37 stopped a bit far,
  # but 0.35 leaves less margin; 0.36 was the conservative closer target.
  # 2026-06-14 (Mr.Kung): stop a touch farther from the bear → 0.38 (~5 cm margin).
  grab_target_distance_m: 0.38
  # grab_commit_forward_seconds: DEPRECATED — superseded by the gap-bounded
  # stop-and-look creep above (the old timed open-loop push drove the body through the bear).
  grab_commit_forward_speed: 220.0
  # Measured forward rate at grab_commit_forward_speed; runtime scales linearly
  # for climb_forward_speed when it must use the far branch.
  grab_creep_mps: 0.11
  # 2026-06-13: forward creep speed for the FAR bear-servo branch (d > grab_distance_m),
  # which doubles as the ramp climb. Higher than control.slow_speed (350) to beat the
  # incline's raised stall floor; scoped here so visual servo / final approach / probe
  # keep using slow_speed. Drops back to grab_commit_forward_speed once near.
  climb_forward_speed: 450.0
  # 2026-06-14 (Mr.Kung): far ramp-bear approach drives CONTINUOUSLY instead of the
  # pulsed stop-and-look creep. While the bear is far, precise alignment doesn't
  # matter, so the camera lag is harmless and the car can just open up and drive at
  # it. CRUISE re-reads the (lagged) bear each tick only to know when to stop:
  #   - cruise_forward_speed: continuous far speed. More aggressive than the pulsed
  #     climb_forward_speed; raise for a faster run-up (Unity stall floor ~250).
  #   - cruise_until_m: stop cruising and hand back to the careful near stop-and-look
  #     stepper once the (lagged) depth reaches this. Keep == grab_distance_m so the
  #     car never cruises into the close-range zone (overshoot from the ~0.3s lag is
  #     bounded and still well above grab_target_distance_m).
  #   - cruise_align_band_px: if heading drifts past this wide band, stop and re-settle
  #     for a precise TURN (the tight align band is only enforced near the bear).
  #   - far_continuous: false restores the old pulsed FORWARD-burst far creep.
  far_continuous: true
  cruise_forward_speed: 600.0
  cruise_until_m: 0.55
  cruise_align_band_px: 150.0
  # Blocking bears are under/near the bridge entrance, not on the ramp climb.
  # Use one-tick, low-speed nudges so losing bbox cannot launch the car onto the bridge.
  clear_blocking_forward_speed: 220.0
  clear_blocking_forward_s: 0.10
  clear_lost_backup_speed: 220.0
  clear_lost_backup_seconds: 0.8
  # 0 = stop fully before/while the arm grabs. Was 120 (a forward creep to fight
  # gravity pulling the chassis back on the ramp), but the IK target is sampled
  # ONCE at trigger from the YOLO marker, then the car kept creeping for the full
  # grab_wait_seconds (~12s) — so the bear's real position drifted away from the
  # locked target and the arm reached for a stale/too-far X (the 0.18m > 0.191m
  # max-reach miss). Stopping removes that motion-vs-stale-depth inconsistency.
  # CAVEAT: if the car now back-slides on the ramp during the grab, restore a
  # small value (e.g. 60) rather than the old 120.
  grab_hold_forward_speed: 0.0
  # 2026-06-13: 0.7→0.3. The servo acts on the freshest dx; a 0.7 s-old reading
  # added phase lag that fed the oscillation. Tighten so stale frames trigger the
  # search/hold branch instead of a confidently-wrong turn.
  grab_info_stale_s: 0.3
  # Safe arm precondition before publishing target_point/clicked_point to auto_arm.
  # Direct /robot_arm degrees, calibrated from /robot_arm echo:
  # [0.0, 141.0, 90.0] = raised claw pose that avoids sweeping through the bear.
  safe_pre_arm_pose_deg: [0.0, 141.0, 90.0]
  # 2026-06-14 (Mr.Kung): DIRECT close-gripper grab — now the PRIMARY grab (not a
  # fallback), replacing the IK auto_grab. At close range the chassis bumps the bear
  # so it is already at the gripper: don't raise the claw first ("舉高爪子再夾"), just
  # lower onto the bumped bear (gripper open), close, then raise to the lift pose
  # holding it. Calibrated [108,120,90] grabs a bottom-centre object at YOLO ~0.39 m.
  # Set grab_direct_close=false to restore the IK auto_grab. Applies to BOTH the
  # blocking-bear clear (CLEAR_BEAR) and the ramp-bear grasp (GRASP_RAMP_BEAR).
  grab_direct_close: false     # 2026-06-14 (Mr.Kung): reverted to IK auto_grab; set true to re-enable direct grab
  grab_direct_pose_deg: [108.0, 120.0, 90.0]   # lower onto the bumped bear, gripper open
  grab_direct_lift_deg: [0.0, 141.0, 90.0]     # raise to the "same position" after closing
  grab_direct_reach_s: 1.5     # wait for the arm to reach the grab pose before closing
  grab_direct_close_s: 1.0     # wait for the gripper to close on the bear
  grab_direct_lift_s: 1.0      # wait for the lift to complete
  # Auto grab wrist/second-axis offset applied inside pros_car's arm_controller_2D.
  # Positive tilts the claw more forward from the IK pose.
  grab_wrist_forward_offset_deg: 40.0
  # Close-range bbox/marker can disappear while the arm raises; hold the grab-pose
  # target snapshot briefly so CLEAR_BEAR does not oscillate forward/backward.
  grab_snapshot_max_age_s: 2.0
  pre_arm_settle_s: 0.5
  # Seconds to wait after triggering the auto_arm_human IK grab (/clicked_point)
  # before depth-probe verification. The grab runs in arm_controller_2D's background
  # thread (open → IK approach → close → retract); too short drops the bear before
  # the gripper closes. Matches get_bear_node's grab_wait_seconds. TUNE to the rig.
  grab_wait_seconds: 12.0
  servo_timeout_s: 600.0      # bear lost during servo for this long → FAILED
  # Auto-retry uses a locked candidate from /yolo/bear_info's multi-bear tail:
  # the bear closest to the pre-grab dx/py/depth is tracked through the probe so
  # other bears in the frame cannot hijack verification.
  # 2026-06-13 (Mr.Kung): temporarily skip the probe/depth verify after grabbing
  # the ramp bear — once the grab wait completes, return straight to origin.
  # Set false to restore the full verify chain (probe + depth-stability check).
  skip_grasp_verify: false
  verify_retry_on_ground: true
  # 2026-06-14 (Mr.Kung): cancelled the retry cap. <0 = unlimited verify
  # retries — a failed verify no longer fails the mission ("grab failed after
  # N failed verifies"); it re-servos and re-grabs until the bear locks (the
  # servo_timeout_s budget still bounds a fully-lost bear). Set back to 2 to
  # restore the bounded cap. Only used when verify_retry_on_ground=true.
  grab_retry_max: -1
  # 2026-06-13: widened lock tolerances. A grabbed bear is held ~0.3 m on the
  # claw and is LIFTED, so its pixel_y jumps well above the pre-grab lock; the
  # old 180 px gate dropped the locked candidate ("no locked candidate") and
  # spuriously failed verify. dx also drifts as the held bear swings.
  verify_lock_dx_tolerance_px: 160.0
  verify_lock_py_tolerance_px: 280.0
  verify_lock_depth_tolerance_m: 0.35
  # 2026-06-13 (Mr.Kung): grab-RETRY target identity. The committed bear's GROUND
  # position is locked in the map frame (back-projected from pixel+depth via
  # camera_info + TF). On a retry the bear servo re-acquires the candidate within
  # this radius of that world spot instead of the new overall-nearest bear, so it
  # can't swing toward a different bear (the wrong-way-turn bug). If no candidate
  # is within radius, the servo re-searches rather than chase the wrong one.
  # Falls back to overall-nearest when camera_info/TF are unavailable (map_pos=None).
  # Wide enough for localization noise, tight enough to exclude a neighbouring bear.
  verify_lock_map_radius_m: 0.30
  camera_frame: "camera_optical_frame"
  camera_info_topic: "/camera/depth/camera_info"
  # Grasp verification: record locked-bear depth, back up a little, then require
  # the same locked bear to stay roughly stable. Tolerances are deliberately wider
  # than raw depth noise because a held bear can swing in the claw.
  verify_baseline_observe_seconds: 0.3
  verify_probe_distance_m: 0.12
  verify_probe_min_distance_m: 0.06
  verify_probe_speed: 220.0
  verify_probe_timeout_s: 0.7
  verify_restore_timeout_s: 0.7
  # 2026-06-13: 0.8→1.2 so the wider min_frames window below has time to collect
  # enough fresh samples without tripping "not enough fresh depth samples".
  verify_observe_seconds: 1.2
  verify_info_stale_s: 0.7
  verify_arm_depth_threshold: 0.65
  # 2026-06-13: 1→2. A held bear swings in the claw, so a single ~0.3 m depth
  # frame can jitter past the stable tolerance and falsely report "depth
  # changed". Averaging 2+ fresh frames smooths the swing.
  verify_depth_min_frames: 2
  # Keep at 0.10: the probe is 0.12 m, so a bear still on the ground shows
  # delta ≈ 0.12 m. Raising this toward/over 0.12 would let a ground bear pass
  # as a successful grasp. Fix swing-induced false fails via min_frames, not here.
  verify_depth_stable_tolerance_m: 0.10
  clear_backward_seconds: 1.0
  clear_rotate_seconds: 1.2

return:
  # RETURN leaves the bridge/ramp BEFORE routing home, and picks the exit
  # direction PURELY from yaw (_select_return_exit_mode) — never from XY map
  # coords. 2026-06-13: the old XY AABB gate read pose (0.86,1.37) as
  # "off-bridge" and sent the car into an in-place rotate on the ramp. z is no
  # help either: map->base_footprint is ground-projected (z≈0 on a ramp per
  # REP-120) and AMCL is 2D, so yaw is the only reliable on-bridge signal.
  # Code mode names are yaw-based:
  #   horizontal (yaw near ±180°) → forward 1.0 m
  #   vertical   (yaw near +90°)  → reverse straight to the saved short-side observe point
  bridge_yaw_select_threshold_deg: 55.0
  bridge_exit_speed: 300.0
  horizontal_forward_m: 1.0
  vertical_reverse_m: 1.5     # fallback only if no saved observe point exists
  observe_reverse_max_extra_m: 0.3
  drop_at_origin: true       # false = hold the bear at origin until task trigger
  drop_wait_seconds: 2.0
  back_away_seconds: 1.0
